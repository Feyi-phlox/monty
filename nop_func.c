#include "monty.h"
/**
 * nop - does not do anything
 * @stack: stack
 * @line_number: line number
 * Return: 0 if success
 */

void nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
